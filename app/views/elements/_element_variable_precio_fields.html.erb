<div class="nested-fields">
    <table class="field">
      <tr>
        <th><%= f.label "Subvariable Precio" %></th>
        <th><%= f.label "valor" %></th>
        <th>&nbsp;</th>
      </tr>
      <tr>
        <td><%= f.collection_select :variable_precio_id, VariablePrecio.all, :id, :detalle, :prompt => true %></td>
        <td><%= f.text_field :valor %></td>
        <td><%= link_to_remove_association "Borrar Subvariable Precio", f %></td>
      </tr>
   </table>
</div>

