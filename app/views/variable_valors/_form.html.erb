<%= form_for(current_empresa_and_proyect_path_for(@variable_valor)) do |f| %>
  <% if @variable_valor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@variable_valor.errors.count, "error") %> prohibited this variable_valor from being saved:</h2>

      <ul>
      <% @variable_valor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label "Proyecto" %><br />
      <%= f.collection_select :project_id, Project.all, :id, :detalle, :prompt => true %>
    </div>
    <div class="field">
      <%= f.label :detalle %><br />
      <%= f.text_field :detalle %>
    </div>
    <div class="field">
      <%= f.label :observaciones %><br />
      <%= f.text_area :observaciones %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
