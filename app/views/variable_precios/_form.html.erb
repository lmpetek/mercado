<%= form_for( current_empresa_and_proyect_path_for(@variable_precio) ) do |f| %>
  <% if @variable_precio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@variable_precio.errors.count, "error") %> prohibited this variable_precio from being saved:</h2>

      <ul>
      <% @variable_precio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <h3>Empresa: <%= @empresa.detalle.titlecase %></h3>
      <h3>Proyecto: <%= @project.detalle.titlecase %></h3>
    </div>

    <div class="field">
      <%= f.label :detalle %><br />
      <%= f.text_field :detalle %>
    </div>
    <div class="field">
      <%= f.label :observaciones %><br />
      <%= f.text_area :observaciones %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
